if (x == y and  a == b) and c == d:
    g = h
else:
    e = f
    
    
# if either of the first condition is true  x, has the same value as y or a has the same value as b, and in addtion, the third  conditon is true c, has the same valuse as d, then g is assigned to the value of h,
#otherwise g is assigned to the value of h otherwise e is assigned to the value of f.


if c == d:
    if x == y:
        g = h
    elif a == b:
        g = h
    else:
        e = f
else: e = f

#Another way of nesred 
